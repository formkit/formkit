name: Tests
on: [push, workflow_dispatch]
jobs:
  jest-run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: pnpm install
      - run: pnpm build all
      - run: pnpm test
