import { createElement } from 'react'

/**
 * Development helper component used by local playgrounds.
 * React parity implementation intentionally keeps this minimal.
 */
export function FormKitKitchenSink() {
  return createElement('div', null)
}
